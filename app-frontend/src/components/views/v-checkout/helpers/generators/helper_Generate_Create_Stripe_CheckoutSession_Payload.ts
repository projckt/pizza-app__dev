export const helper_Generate_Create_Stripe_CheckoutSession_Payload = (id_Document: string) => {
  let payload_Create_Stripe_CheckoutSession = {
    id_Document: id_Document,
  };

  return payload_Create_Stripe_CheckoutSession;
};
